#pragma once

#include <random>
#include <ctime>
#include <iostream>
using namespace std;

int* multiply(int*, int*, int, int);

void division(int*, int, int*, int, int*);

void reverse(int*, int);

void generate_e(int*, int);

void generate_random_m(int*, int);

int* coder(const int, int*, const int, int*);

bool decoder(int*, int, int, int*, int);